[artifacts]
path = "artifacts"

[network]
reserve-ports = 1

[systemd-unit]
template = "builtin://application"
exec-start = "{{ APP_ACTIVE_ARTIFACTS_DIR }}/guestbook"

[caddyfile]
template = "builtin://reverse-proxy"
domains = ["guestbook.example.com"]
