[artifacts]
path = "artifacts"

[network]
reserve-ports = 1

[systemd-unit]
template = "builtin://application"
exec-start = "{{ APP_ACTIVE_ARTIFACTS_DIR }}/goatcounter serve -listen :{{ APP_PORT }}"

[caddyfile]
template = "builtin://reverse-proxy"
domains = ["goats.example.com"]
